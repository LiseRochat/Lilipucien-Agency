{% extends 'base.html.twig' %}

{% block title %}Status{% endblock %}

{% block body %}
<div class="container">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ status.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ status.description }}</td>
            </tr>
        </tbody>
    </table>
    
    <div>
        <a class="btn btn-dark my-3" href="{{ path('app_status_edit', {'id': status.id}) }}">
            <i class="bi bi-pen"></i>
            Modifier
        </a>
    </div>
   
    <div>
        <a class="btn btn-light my-3" href="{{ path('app_status_index') }}">
            <i class="bi bi-arrow-left-circle"></i>
            Retourner à la liste des Status
        </a>
    </div>
</div>

{% endblock %}
