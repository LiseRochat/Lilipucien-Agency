{% extends 'base.html.twig' %}
{% block title %}Nos Biens Immobiliers | Agence Lilipucien{% endblock %}
{% block body %}
	<nav aria-label="breadcrumb">
  		<ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Accueil</a></li>
            <li class="breadcrumb-item active" aria-current="page">Administration</li>
    		<li class="breadcrumb-item active" aria-current="page">Accueil</li>
  		</ol>
	</nav>
    <div class="container px-4 px-lg-5 my-5">
        <a class="btn btn-success my-3" href="{{ path('app_status_add') }}">
            <i class="bi bi-plus-circle"></i>
                Ajouter un status
        </a>
        <table class="table table-striped-columns">
            <thead>
                <tr>
                    <th scope="col">n°</th>
                    <th scope="col">Nom</th>
                    <th scope="col">Description</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for statusOne in status %}
                <tr>
                    <th scope="row">{{ statusOne.id }}</th>
                    <td>{{ statusOne.title }}</td>
                    <td>{{ statusOne.description }}</td>
                    <td> 
                        <a class="btn btn-secondary" href="{{ path('app_status_edit', {id : bien.id }) }}">
                            <i class="bi bi-pen"></i>
                                Modifier le status
                        </a>
                        <a class="btn btn-danger" href="{{ path('app_status_delete', {id : bien.id }) }}">
                            <i class="bi-trash me-1"></i>
                                Supprimez le status
                        </a> 
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}